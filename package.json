{
  "name": "csic-backend",
  "private": true,
  "workspaces": [
    "csic_frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=csic_frontend",
    "dev": "npm run dev --workspace=csic_frontend"
  }
}
